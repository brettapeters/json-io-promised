{
  "name": "json-io-promised",
  "version": "1.0.0",
  "description": "Promise wrapper around fs for reading/writing JSON",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "node_modules/.bin/tape ./test/*.js | tap-spec",
    "coverage": "node_modules/.bin/istanbul cover node_modules/.bin/tape ./test/*.js | tap-spec"
  },
  "author": "Brett Peters",
  "license": "ISC",
  "devDependencies": {
    "istanbul": "^0.4.5",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3"
  }
}
